package com.example.imagecomp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary palette — deep indigo
val Primary = Color(0xFF4F46E5)
val PrimaryLight = Color(0xFF818CF8)
val PrimaryDark = Color(0xFF3730A3)

// Secondary — teal accent
val Secondary = Color(0xFF0D9488)
val SecondaryLight = Color(0xFF5EEAD4)

// Score colors
val ScoreHigh = Color(0xFF22C55E)      // ≥ 0.7
val ScoreMedium = Color(0xFFF59E0B)    // ≥ 0.5
val ScoreLow = Color(0xFFEF4444)       // < 0.5

// Surface / background
val SurfaceDark = Color(0xFF1E1B4B)
val SurfaceLight = Color(0xFFF8FAFC)
val Background = Color(0xFFFFFFFF)

// On-colors
val OnPrimary = Color.White
val OnSecondary = Color.White
