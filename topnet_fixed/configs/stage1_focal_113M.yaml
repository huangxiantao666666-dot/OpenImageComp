# Experiment B (113M scaled-up): Partial Focal Loss, 113M params
# python train_stage1.py --config configs/stage1_focal_113M.yaml

data_dir: "./data/data"
checkpoint_dir: "./checkpoints/expB_focal_113M"
log_dir: "./logs/expB_focal_113M/stage1"

model_type: "ObPlaNet_resnet18_keypoint_113M"
freeze_encoders: true

data_type: "gaussian"
image_size: 256
batch_size: 32
num_workers: 4

epochs: 10
optimizer: "adamw"
lr: 1.0e-4
weight_decay: 1.0e-4

loss: "focal"
focal_alpha: 2.0
focal_beta: 4.0
gaussian_sigma_factor: 8.0
focal_full_supervision: false

patience: 8
early_stop_metric: "val_loss"
early_stop_delta: 1.0e-4

print_freq: 100
tensorboard: true
